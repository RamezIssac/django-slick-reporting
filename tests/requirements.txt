-r ../requirements.txt
crispy-bootstrap4
# TOON (Token-Oriented Object Notation) serializer for the llm data-format
# evaluation. Pinned to the beta: the stable 0.1.0 has no encoder implemented
# (NotImplementedError); 0.9.0b1 encodes/decodes spec-canonically.
toon-format==0.9.0b1
